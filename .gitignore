/out
.DS_Store
.blog-build-packages
/draft
ltximg
/result
/.direnv
/.org-timestamps
/src/index.org
/src/tags

# build-time post-processing deps (scripts/postprocess.ts)
/node_modules

# Link-card OGP/GitHub metadata: derived from the working tree (which may hold
# unpublished drafts), so not committed. Regenerated by `nix run .#linkcard`
# (CI, from the committed tree) and by `just build` locally.
/linkcard-cache.json
