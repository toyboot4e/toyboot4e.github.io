name: "Test"

on:
  pull_request:

jobs:
  build:
    name: Build test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build devlog
      uses: ./.github/actions/build
